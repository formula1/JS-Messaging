'use strict';

module.exports.parseText = function(t, text){

};

module.exports.getExpectedValues = function(type){
  switch(type){
    case 'trigger': return { value: Math.random() };
    case 'trigger': return { value: Math.random() };
  }
};
